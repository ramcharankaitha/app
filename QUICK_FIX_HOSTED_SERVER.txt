URGENT: Fix Email Constraint on Hosted Server

STEP 1: Run this SQL on your hosted database (Railway/Render/etc.):

ALTER TABLE staff DROP CONSTRAINT IF EXISTS staff_email_key;
ALTER TABLE staff ALTER COLUMN email DROP NOT NULL;

ALTER TABLE users DROP CONSTRAINT IF EXISTS users_email_key;
ALTER TABLE users ALTER COLUMN email DROP NOT NULL;

ALTER TABLE customers DROP CONSTRAINT IF EXISTS customers_email_key;
ALTER TABLE customers ALTER COLUMN email DROP NOT NULL;

STEP 2: Deploy the updated code (already fixed in staff.js and users.js)

The code now:
- Removes email from all INSERT statements
- Automatically fixes constraints on server start
- Fixes constraints before each insert attempt
- Has retry logic if constraint error occurs

After running SQL + deploying code, staff creation will work without email!

